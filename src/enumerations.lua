-- --- @enum CARD_TARGET_MODE
-- CARD_TARGET_MODE = {
-- 	--- The card will automatically snap if there's only one card slot it can be slotted into.
-- 	AUTOMATIC = 'automatic',

-- 	--- The player has to manually drag the card over the card slot.
-- 	MANUAL = 'manual'
-- }
